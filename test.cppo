#define debug(s) Printf.eprintf "%S %i: %s\n%!" __FILE__ __LINE__ s

#define pi 3.14
f(1)
#define f(x) x+pi
f(2)
#undef pi
f(3)

#ifdef g
"g" is defined
#else
"g" is not defined
#endif


#define a(x) b()
#define b(x) a()
a()

debug("a")
debug("b")

#define z 123
#define y z
#define x y

#if x lsl 1 = 2*123

#if 1 = 2
#error "test"
#endif

success
#else
failure
#endif

#define test_esc \
"abc\
 def" \
(* 123 \
   456 *)
test_esc
